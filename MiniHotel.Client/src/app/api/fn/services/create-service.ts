/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ServiceDto } from '../../models/service-dto';
import { ServiceUpsertDto } from '../../models/service-upsert-dto';

export interface CreateService$Params {
  
    /**
     * The service creation data transfer object.
     */
    body?: ServiceUpsertDto
}

export function createService(http: HttpClient, rootUrl: string, params?: CreateService$Params, context?: HttpContext): Observable<StrictHttpResponse<ServiceDto>> {
  const rb = new RequestBuilder(rootUrl, createService.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ServiceDto>;
    })
  );
}

createService.PATH = '/api/Services';
