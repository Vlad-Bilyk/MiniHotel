/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RoomDto } from '../../models/room-dto';

export interface RoomsGetAvailableRooms$Plain$Params {

/**
 * The start date of the desired booking period.
 */
  startDate: string;

/**
 * The end date of the desired booking period.
 */
  endDate: string;
}

export function roomsGetAvailableRooms$Plain(http: HttpClient, rootUrl: string, params: RoomsGetAvailableRooms$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<RoomDto>>> {
  const rb = new RequestBuilder(rootUrl, roomsGetAvailableRooms$Plain.PATH, 'get');
  if (params) {
    rb.path('startDate', params.startDate, {});
    rb.path('endDate', params.endDate, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<RoomDto>>;
    })
  );
}

roomsGetAvailableRooms$Plain.PATH = '/api/Rooms/available/{startDate}/{endDate}';
