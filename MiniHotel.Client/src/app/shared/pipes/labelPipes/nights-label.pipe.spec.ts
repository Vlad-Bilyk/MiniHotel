import { NightsLabelPipe } from './nights-label.pipe';

describe('NightsLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new NightsLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
