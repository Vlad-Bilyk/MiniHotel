import { RoomLabelPipe } from './room-label.pipe';

describe('RoomLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new RoomLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
